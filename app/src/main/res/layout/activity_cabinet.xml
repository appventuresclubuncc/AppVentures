<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_cabinet"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="64dp"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.uncc.appventures.CabinetActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="horizontal"></android.support.v7.widget.RecyclerView>

    <LinearLayout
        android:id="@+id/facts"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingLeft="16dp"
        android:paddingTop="16dp"
        android:visibility="invisible">

        <TextView
            android:id="@+id/memName"
            style="@style/infoStyles"
            tools:text="Name" />

        <TextView
            android:id="@+id/pos"
            style="@style/infoStyles"
            tools:text="Position" />

        <TextView
            android:id="@+id/major"
            style="@style/infoStyles"
            tools:text="Major" />

        <TextView
            android:id="@+id/concentration"
            style="@style/infoStyles"
            tools:text="Concentration" />

        <TextView
            android:id="@+id/reason"
            style="@style/infoStyles"
            tools:text="Reason for Being Here" />

        <TextView
            android:id="@+id/grad"
            style="@style/infoStyles"
            tools:text="Graduation Date" />
    </LinearLayout>
</LinearLayout>
